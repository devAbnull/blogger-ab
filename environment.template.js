const envConfig = {
  dbUser: '*****', // db username
  dbPassword: '******', // db password
  dbUrl: '*******', // db url,
  port: 8000,
  adminPassword: '*******',
  isDebug: false,
};

module.exports = envConfig;